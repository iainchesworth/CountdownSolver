# ---------------------------------------------------------------------------
# countdown_app - the Qt6 Quick (QML) GUI. All game logic lives in
# countdown::solver; this target is presentation + platform glue only.
# ---------------------------------------------------------------------------
find_package(Qt6 6.5 REQUIRED COMPONENTS Quick Gui)

qt_standard_project_setup(REQUIRES 6.5)

# Select the single platform implementation for the target OS. No #ifdefs: CMake
# decides which translation unit is compiled.
if(WIN32)
    set(COUNTDOWN_PLATFORM_SOURCE platform/windows/platform_windows.cpp)
elseif(APPLE)
    set(COUNTDOWN_PLATFORM_SOURCE platform/macos/platform_macos.cpp)
elseif(UNIX)
    set(COUNTDOWN_PLATFORM_SOURCE platform/linux/platform_linux.cpp)
else()
    message(FATAL_ERROR "Unsupported target platform")
endif()

qt_add_executable(countdown_app
    main.cpp
    solver.cpp
    solver.hpp
    platform/platform.hpp
    ${COUNTDOWN_PLATFORM_SOURCE})

qt_add_qml_module(countdown_app
    URI Countdown
    VERSION 1.0
    QML_FILES
        qml/Main.qml
        qml/Theme.qml
        qml/AppState.qml
        qml/components/Card.qml
        qml/components/SectionLabel.qml
        qml/components/Tile.qml
        qml/components/PadButton.qml
        qml/components/FlatButton.qml
        qml/components/NavItem.qml
        qml/components/SegControl.qml
        qml/pages/NumbersPage.qml
        qml/pages/LettersPage.qml
        qml/pages/ConundrumPage.qml
        qml/pages/SettingsPage.qml)

# Theme and AppState are QML singletons.
set_source_files_properties(qml/Theme.qml qml/AppState.qml
    PROPERTIES QT_QML_SINGLETON_TYPE TRUE)

target_include_directories(countdown_app PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}")

target_link_libraries(countdown_app
    PRIVATE
        countdown::solver
        countdown::warnings
        countdown::sanitizers
        Qt6::Quick
        Qt6::Gui)

# The version header is generated by the library's build-time stamp target.
add_dependencies(countdown_app countdown_version)

# std::getenv is flagged "unsafe" by the MSVC CRT; the usage is correct and
# portable, so silence that specific deprecation for this target only.
target_compile_definitions(countdown_app PRIVATE
    "$<$<CXX_COMPILER_ID:MSVC>:_CRT_SECURE_NO_WARNINGS>")

set_target_properties(countdown_app PROPERTIES
    WIN32_EXECUTABLE ON
    MACOSX_BUNDLE ON)

# ---------------------------------------------------------------------------
# Install + Qt runtime deployment. qt_generate_deploy_app_script() is Qt's
# CMake-native replacement for windeployqt/macdeployqt: it inspects the built
# target and emits an install(SCRIPT) that copies the Qt libraries/plugins the
# app actually uses next to it (or into the .app bundle on macOS). Linux has
# no supported deploy step here, so NO_UNSUPPORTED_PLATFORM_ERROR turns that
# into a no-op instead of a configure error; Linux packages rely on a system
# Qt6 install (see the DEB dependency in cmake/Packaging.cmake).
# ---------------------------------------------------------------------------
include(GNUInstallDirs)

install(TARGETS countdown_app
    RUNTIME DESTINATION "${CMAKE_INSTALL_BINDIR}"
    BUNDLE DESTINATION ".")

qt_generate_deploy_app_script(
    TARGET countdown_app
    OUTPUT_SCRIPT COUNTDOWN_APP_DEPLOY_SCRIPT
    NO_UNSUPPORTED_PLATFORM_ERROR)
install(SCRIPT "${COUNTDOWN_APP_DEPLOY_SCRIPT}")
